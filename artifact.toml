kind = "web"
previewPath = "/"
title = "Calendar AI Agent"
version = "1.0.0"
id = "artifacts/calendar-agent"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21432

[services.development]
run = "pnpm --filter @workspace/calendar-agent run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/calendar-agent", "run", "build" ]
publicDir = "artifacts/calendar-agent/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21432"
BASE_PATH = "/"
